VITE_API_BASE=http://localhost:8011/api/v1

# Cloudflare Turnstile SITE key (public, safe to ship to the browser).
# Leave empty locally: the lead form then skips the widget and sends the stub
# token the API accepts while its own TURNSTILE_SECRET is unset.
VITE_TURNSTILE_SITE_KEY=
